<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.how2java.springboot.dao.BudPurchaseDAO">
    <resultMap id="budOrder" type="Map" >
        <result property="id" column="id" />
        <result property="quantity" column="quantity"  />
        <result property="total_price" column="total_price"  />
        <result property="create_time" column="create_time"  />
        <result property="creator_id" column="creator_id"  />
        <result property="remark" column="remark"  />
    </resultMap>
    <select id="list" resultMap="budOrder" parameterType="java.util.Map">
        SELECT * FROM g_bud_order ORDER BY create_time DESC
    </select>

    <insert id="add" parameterType="Map" >
        insert into g_bud_order ( id,quantity,total_price,creator_id,create_time ) values (#{id},#{quantity},#{total_price},#{creator_id},#{create_time})
    </insert>

    <!--<delete id="deleteById" parameterType="java.lang.String">-->
        <!--DELETE FROM g_bud_order WHERE id=#{id}-->
    <!--</delete>-->

    <!--<update id="updateById" parameterType="java.util.Map" >-->
        <!--update g_bud_order-->
        <!--<set>-->
            <!--<if test="_parameter.containsKey('quantity')">-->
                <!--quantity=#{quantity},-->
            <!--</if>-->
            <!--<if test="_parameter.containsKey('total_price')">-->
                <!--total_price=#{total_price},-->
            <!--</if>-->
            <!--<if test="_parameter.containsKey('delivery_id')">-->
                <!--delivery_id=#{delivery_id},-->
            <!--</if>-->
            <!--<if test="_parameter.containsKey('modify_time')">-->
                <!--modify_time=#{modify_time},-->
            <!--</if>-->
            <!--<if test="_parameter.containsKey('modifier_id')">-->
                <!--modifier_id=#{modifier_id},-->
            <!--</if>-->
            <!--<if test="_parameter.containsKey('remark')">-->
                <!--remark=#{remark},-->
            <!--</if>-->
        <!--</set>-->
        <!--where id=#{id}-->
    <!--</update>-->

    <!--<select id="findOneById" resultMap="budOrder" parameterType="java.lang.String">-->
        <!--SELECT * FROM g_bud_order WHERE id=#{id}-->
    <!--</select>-->
</mapper>